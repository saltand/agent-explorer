# Agent Explorer PRD

## 会话用量与执行过程分析

- 记录日期：2026-09-22
- 状态：第一项“用量模型与适配口径统一”已在工作区实现，尚未发版；其余需求待实施。
- 参考：DeepSeek Harness 的 `packages/client/ui-trajectory`，本地仓库位于 `/Users/salt/Code/coding-agents/deepseek-harness`。

### 目标

在现有本地日志浏览能力上，让用户能够回答：一次会话消耗了多少 token、缓存利用情况如何、哪些请求或工具耗时较长，以及执行期间上下文发生了什么变化。

保留多 agent 适配、本地处理、原始数据可追溯和长会话浏览性能。各项指标仅在日志提供足够证据时展示。

### 当前基础

- 已有会话库、跨会话全文搜索、事件时间顺序列表、对话视图及选择联动。
- 已有事件分类过滤、请求关联高亮、工具调用与结果卡片及配对高亮。
- 已有会话元信息、事件摘要、原始 JSON、单事件 token 用量和费用估算。
- Claude 和 Pi 适配器已映射部分用量字段；Codex 用量尚未接入统一展示。
- 用量模型已区分未知与零，支持总输入、普通输入、缓存读取／写入、推理与非推理输出；会话累计分析待实施。

### 实施进度

2026-09-22：完成用量模型与适配口径统一。

- Claude、Pi 共用标准化逻辑，缺失字段不再补零；Pi 接入日志中可选的 `reasoning` 字段。
- 普通输入不包含缓存；总输入仅在三个输入分类均已知时推导。标准化逻辑也支持将明确包含缓存的输入总量拆分为普通输入，为后续适配提供基础。
- 每个已记录或派生字段保留原始事件路径；负数、非整数、超出安全整数范围及不一致的计数会产生可见提示。
- 用量面板增加总输入、推理／非推理输出、字段来源与缺失值说明。
- 费用按互不重叠的分类计算；保留部分定价，缺少用量或价格时只展示不完整的小计。总输入和推理输出不会再次计费。
- 本次不包含请求去重、会话汇总、缓存命中率、Codex 用量接入或日志记录费用展示。

口径依据：[Claude 缓存用量说明](https://platform.claude.com/docs/en/build-with-claude/prompt-caching#tracking-cache-performance)、[Pi Usage 类型](https://github.com/badlogic/pi-mono/blob/main/packages/ai/src/types.ts)、[Pi Responses 用量转换](https://github.com/badlogic/pi-mono/blob/main/packages/ai/src/api/openai-responses-shared.ts)。这些来源用于确认输入分类及推理 token 的包含关系，不作为历史请求的实际计费依据。

### 功能与优先级

| 优先级 | 功能 | 需求 |
| --- | --- | --- |
| P0 | 用量口径统一 | 区分普通输入、缓存读取、缓存写入、输出总量与推理输出；保留缺失状态与原始字段来源。 |
| P0 | 请求、轮次和会话统计 | 展示本次请求、截至该请求的累计值、轮次及会话汇总；支持定位消耗较大的请求。 |
| P0 | 缓存分析 | 展示缓存读取、写入数量；分母已知且口径一致时提供缓存命中率。 |
| P0 | Codex 用量接入 | 识别日志中的用量事件，核对请求关联、增量与累计语义，避免重复统计。 |
| P1 | Turn／Request 分组 | 将用户输入、模型请求、助手输出和关联工具组织起来，支持单独及批量折叠，并保留原始事件定位。 |
| P1 | 工具调用检查 | 在现有配对基础上，将参数、结果、状态、结构化错误和可用的耗时集中展示；有明确父子关系时展示嵌套调用。 |
| P1 | 耗时时间线 | 展示模型请求与工具执行的时间分布；支持等宽／耗时模式、缩放、区间选择，以及点击定位事件。 |
| P1 | 请求性能指标 | 数据充分时展示开始时间、总耗时、TTFT、生成耗时和输出速度，并说明计时依据。 |
| P2 | 上下文与压缩检查 | 展示已记录的系统提示词、工具定义及其变更 Diff；展示压缩事件、状态及可用的用量。 |
| P2 | 代码专用检查器 | 对能够可靠识别的代码执行调用提供行号、高亮、换行、原样复制，以及源码／原始参数／输出切换。 |

### 用量与费用规则

1. **未知不等于零。** 缺失字段显示“未提供”，明确记录的零显示为零。无法判断缓存使用情况时，不标记为“未使用缓存”。
2. **先统一输入口径。** 明确每个 agent 的输入 token 是否包含缓存读取或写入；统一展示后不得重复相加，费用计算也使用同一口径。
3. **区分输出组成。** 仅当输出总量明确包含推理 token，且两者均已知时，才能通过相减得到正文 token；否则只显示已知指标。
4. **累计值去重。** 识别单次用量、流式增量、重复快照和会话累计值。累计快照不得直接逐行求和；不能在未验证语义时将 `last_token_usage` 当作每轮唯一用量。
5. **请求关联可追溯。** 优先使用已记录的请求 ID、调用 ID 和明确的生命周期关系；关联不确定的用量单独标示，不强行归入最近一条助手消息。
6. **统计范围透明。** 区分所选请求、截至该请求、当前轮次与整个会话。历史未完整加载、部分请求缺少用量或存在无法归属的记录时，汇总必须标注范围和完整性。
7. **缓存命中率有条件展示。** 采用“缓存读取 token ÷ 总输入 token”，仅在两个值均已知、口径一致且总输入大于零时计算；缓存写入不算命中。
8. **费用标明来源。** 按模型价格计算的值标记为“估算费用”；日志自带费用标记为“日志记录费用”，不能未经核实称为账单实扣。混合模型分别计价，缺失定价时说明汇总不完整。
9. **保留原始证据。** 每个请求级统计可定位到相关原始事件；异常计数或不一致的快照应标记，不静默修正为看似正常的值。

### 各 agent 的数据接入约束

| Agent | 当前证据与后续要求 |
| --- | --- |
| Claude | 已有输入、输出、缓存读取和写入映射；继续核对多条消息及请求级去重规则。 |
| Pi | 已有输入、输出、缓存读取和写入映射；日志中可包含 `cost`，采用前需核实语义，并与估算费用区分。 |
| Codex | 已观察到 `token_count` 的累计与最近用量，以及 `token_usage_record`；实现前核对二者关系、重复快照及输入包含缓存的语义。 |
| Grok | 用量字段目前在 `unified.jsonl` 诊断日志中发现；不能视为当前导入的 `chat_history.jsonl` 已有这些数据。先验证两种日志能否可靠关联，再决定接入。 |
| Cursor | 已检查的样本尚未确认可用的 token 字段；保留未知状态，不据此断言所有 Cursor 日志均不提供用量。 |

### 时间线与计时规则

- 通过明确关联的开始、完成事件计算请求或工具耗时；不能把相邻消息的时间差直接当成执行耗时。
- TTFT 需要明确的起点与首 token 时间，展示时说明是请求开始还是步骤开始，以及是否包含重试等待。
- 生成耗时需要首 token 和结束时间；输出速度使用相同范围的输出 token 除以生成秒数。
- 未完成记录只显示运行状态及已知开始时间，不将经过时间冒充最终耗时。
- 缺少时间戳时仍可按事件顺序浏览；不补造时间跨度。
- 时间线与事件列表共享选择状态，搜索或折叠后仍能定位关联记录。

### 浏览性能要求

- 用量与分组采用增量统计，避免每次流式更新重新扫描整个会话。
- 详情、代码高亮和 Diff 按需生成；长列表继续使用虚拟化。
- 流式追加时仅在用户位于尾部时自动跟随，向上浏览时保留当前位置。
- 使用固定日志语料对比变更前后打开会话、追加更新、搜索和内存占用；记录语料规模、运行环境及测量口径，避免不同语料直接比较百分比。

### 实施顺序

第一批依次完成：用量模型与适配口径统一 → Codex 用量接入 → 请求及会话统计 → Turn／Request 分组。请求级统计应先建立可追溯的关联关系，分组 UI 随后呈现这些关系。

第二批完善工具调用检查、耗时时间线与有可靠数据支撑的性能指标。第三批增加上下文 Diff、压缩检查和代码专用检查器。

Grok 诊断日志关联和 Cursor 用量发现作为独立调研项，不阻塞已有数据来源的功能交付。

### 验收要点

- 用固定样本核对各 agent 的单请求与累计用量；覆盖缺失字段、明确零、重复快照、累计重置、混合模型及不完整历史。
- 同一请求的多条记录不会重复计费；包含缓存的输入总量不会与缓存再次相加。
- 缓存命中率、正文 token 和费用仅在满足计算条件时出现，用户能辨认未知或不完整统计。
- 分组与工具检查可返回原始事件；未匹配或中断的调用仍可见。
- 计时不足时显示不可用，重试及并行调用不会产生误导性的耗时归属。
- 长会话的搜索、选择联动、流式追加和滚动位置保持正常，并提供同一语料上的性能对比。

### 本阶段边界

本文记录功能规划，不包含自动提交、发布或版本号变更。暂不承诺各 agent 都具备同等深度的分析数据，也不包含任务重放、重新执行工具或修改源会话日志。
